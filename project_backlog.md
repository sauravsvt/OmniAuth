# OmniAuth Core Backlog

## P0: Critical Security & Infrastructure

- [x] **Integrate `zeroize` crate**:
    - Key-encryption keys (KEK) and plaintext identity buffers wiped via `Zeroizing` wrappers.
    - Target: `oss/crypto-core/src/lib.rs` — DONE
- [x] **Implement Argon2id**:
    - KEK derived from master password via Argon2id (not used directly as encryption key).
    - Target: `oss/crypto-core/src/lib.rs` (Vault::new) — DONE
- [x] **ZK Circuit Soundness**:
    - Bit decomposition added to all slack variables (64-bit) to prevent finite-field wrap-around.
    - Negative tests confirm minors and out-of-range values are correctly rejected.
    - Target: `oss/crypto-core/src/zk/circuits.rs` — DONE
- [x] **Mobile Native Bridge Alignment**:
    - Kotlin and Swift modules rewritten to match current Rust Vault API (no stale `unlock()`/`Identity`).
    - CryptoService.ts updated with proper async/await and password-per-operation pattern.
    - Target: `oss/client-mobile/` — DONE

## P1: Testing & Assurance

- [ ] **End-to-End (E2E) Handshake Tests**:
    - Goal: Verify full flow: Mobile -> PQC Sign -> Backend Verify -> Auth Success.
    - Target: New integration test suite
- [ ] **Rust-to-Go Interop Tests**:
    - Goal: Enable `generate_interop_vectors` and validate Go can verify Rust-generated signatures.
    - Blocked on: FIPS library encoding alignment between `pqcrypto-dilithium` and CIRCL `mode3`.
- [ ] **ZK Interop Tests**:
    - Goal: Go backend verifies Groth16 proofs generated by Rust.
    - Target: `proprietary/backend/` — needs arkworks-compatible Go verifier or WASM bridge.

## P2: Future Features

- [ ] **Key Rotation**:
    - Goal: Implement logic for replacing old Dilithium keys without losing identity continuity.
    - Approach: Threshold PQC (t-of-n Dilithium signing) for social recovery.
- [ ] **Biometric Unlock**:
    - Goal: Store master password in iOS Keychain / Android KeyStore, unlock with FaceID/fingerprint.
- [ ] **Formal Verification**:
    - Goal: Tamarin/ProVerif model of the challenge-response protocol.
    - Proves: No replay attacks, forward secrecy, key compromise impersonation resistance.
- [ ] **ZK Trusted Setup Ceremony**:
    - Goal: Replace per-instance `circuit_specific_setup` with stable proving/verifying keys from a Powers of Tau ceremony.
- [ ] **HQC Integration**:
    - NIST selected HQC (March 2025) as backup to ML-KEM. Consider as fallback KEM.
- [ ] **Publish `omniauth_core` to crates.io**:
    - Requires: Security audit, stable API, proper documentation.
